import styled from "styled-components";

const Header = styled.div`
  background: cyan;
  padding: 1em;
  justify-content: space-between;
  display: flex;
  item-align: center ul {
    display: flex;
    justify-content: space-between;
    margin-right: 10px;
  }
  li {
    margin: 0 20px;
    text-decoration: none;
  }
`;

export default Header;
